apiVersion: v1
kind: Pod
metadata:
  name: custom-pod
  labels:
    app: web
    environment: staging
    team: backend
spec:
  containers:
  - name: web-app
    image: nginx:latest
    ports:
    - containerPort: 80
